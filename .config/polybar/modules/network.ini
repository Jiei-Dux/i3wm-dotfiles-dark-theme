[module/network]
type				= internal/network

interface			= wlan0
interface-type			= wireless
interval			= 3

format-connected		= <label-connected> Connected~
format-connected-background	= ${colors.background-module}
label-connected			= %{T2}%{T-}

format-disconnected		= <label-disconnected> Disconnected TvT
format-disconnected-background	= ${colors.background-module}
label-disconnected		= %{T2}󰖪%{T-}
